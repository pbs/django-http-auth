[tox]
envlist=pytest

[testenv:pytest]
install_command=
    pip install -b .tox/build-pytest {opts} {packages}
deps=
    pytest-django
changedir=
    {envdir}
commands=
    py.test \
        --ds=multisiteauth.tests.settings \
        --junitxml={toxinidir}/pytest-results.xml \
        --pyargs \
        {posargs:multisiteauth.tests.tests}
